[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"
rootDirectory = "gokulam_backend"

[deploy]
startCommand = "python manage.py migrate --noinput && python seed_data.py && gunicorn gokulam_backend.wsgi --bind 0.0.0.0:$PORT"
